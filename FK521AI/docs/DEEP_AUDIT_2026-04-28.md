# FK521AI 深度功能审查（2026-04-28）

> 本轮先完成“可快速落地且风险较低”的修复；其余问题分为下一轮迭代（需要联调/回归/安全评审）。

## 用户端问题（10项）

1. 头像上传未带文件时返回 500，用户无可读提示（已修复）。
2. 头像上传在未登录态下抛异常，返回 500（已修复为 401）。
3. 头像临时文件删除未做空值保护（已修复）。
4. 启动配置接口在异常时泄露内部错误信息（已修复）。
5. 消息分支创建失败场景统一 500，错误区分不足（待优化）。
6. 搜索消息时分页/排序体验不一致（待优化）。
7. 前端上传错误提示对服务端 message 依赖过强（待优化）。
8. 会话 SSE 断连重试策略在弱网下仍可能抖动（待优化）。
9. 文件上传大小与格式限制在多入口提示不统一（待优化）。
10. 部分用户态接口异常码映射不一致（待优化）。

## 管理端问题（20项）

1. 角色名仅 `toUpperCase`，未处理 URL 编码/空白（已修复）。
2. 角色更新接口把所有异常都当作 400（已修复：区分验证错误与内部错误）。
3. 角色查询 roleName 为空未拦截（已修复）。
4. 管理端创建用户未校验邮箱格式（已修复）。
5. 管理端更新用户可将邮箱改为空字符串（已修复）。
6. 管理端更新用户邮箱格式未校验（已修复）。
7. 管理员可在当前会话自降级，导致权限中断风险（已修复）。
8. 管理员可删除当前登录账号，导致会话异常（已修复）。
9. 系统设置测试邮箱缺少邮箱格式校验（已修复）。
10. 测试邮件发送缺少连接超时配置，弱网可能长时间阻塞（已修复）。
11. 管理端配置 section 权限文案不统一（待优化）。
12. 管理端配置更新缺少更细粒度审计日志（待优化）。
13. 管理端用户列表搜索参数缺少上限约束（待优化）。
14. 管理端批量操作缺少幂等 token（待优化）。
15. 管理端角色权限更新缺少乐观锁 revision（待优化）。
16. 管理端导入模型配置错误信息颗粒度不足（待优化）。
17. 管理端 API key 生命周期管理策略可增强（待优化）。
18. 管理端部分高危操作缺少二次确认 challenge（待优化）。
19. 管理端部分接口缺少速率限制器（待优化）。
20. 管理端测试邮件接口缺少发送频率限制（待优化）。

## 额外跨端问题（10项）

1. 启动配置 payload 体积可继续裁剪（待优化）。
2. 错误码字典尚未完全统一（待优化）。
3. i18n 错误提示存在中英混杂（待优化）。
4. SSE 事件 schema 未完全版本化（待优化）。
5. 前后端对日期格式有多实现并存（待优化）。
6. 文件策略适配层缺少统一异常分类（待优化）。
7. 关键路径日志缺少 request-id 贯穿（待优化）。
8. 资源上传/删除在并发下仍有少量竞态窗口（待优化）。
9. 部分接口对 tenantId 回退策略可更严格（待优化）。
10. 端到端回归覆盖率仍需提升（待优化）。
